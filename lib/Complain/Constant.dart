import 'package:flutter/material.dart';

final decoration = BoxDecoration(
  borderRadius: BorderRadius.circular(5.0),
  color: Colors.white,
);

final margin = EdgeInsets.only(left: 10, right: 10, top: 10);
final Color color = Colors.black54;
